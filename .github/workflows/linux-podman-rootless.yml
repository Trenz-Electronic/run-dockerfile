name: Linux · Podman (rootless)

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  cell:
    uses: ./.github/workflows/cell.yml
    with:
      runner-json: '"ubuntu-latest"'
      engine: podman
      userns: keep-id
      cell-caps: 'qemu-amd64 qemu-arm64 qemu-armv7 cgroups gnu-stat python3 linux rootless-podman'
      setup-qemu: true
      setup-podman: true
